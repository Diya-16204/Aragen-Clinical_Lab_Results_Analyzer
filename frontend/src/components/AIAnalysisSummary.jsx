import React from 'react'

export default function AIAnalysisSummary({ summary }) {
  if (!summary) return null
  const failed = summary.status !== 'success'
  return <section className={`ai-summary ${failed ? 'unavailable' : ''}`}><div className="summary-title"><div><span className="eyebrow">AI-SUPPORTED SYNTHESIS</span><h2>AI Analysis Summary</h2></div><span className="ai-source">✦ {failed ? 'Unavailable' : `Generated by ${summary.generated_by || 'AI'}`}</span></div>{failed ? <div className="summary-fallback"><strong>AI summary unavailable</strong><p>Your deterministic lab analysis, reference ranges, and individual findings are still available below.</p></div> : <><p className="summary-text">{summary.text}</p><p className="summary-disclaimer">Informational decision support only. This does not replace professional clinical judgment.</p></>}</section>
}
